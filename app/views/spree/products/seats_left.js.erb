$("#seats_left").text("<%= @available_bus.empty? ? 'No seats available.' : @available_bus[:seats_left ] %>");
$("#bus_id").val("<%= @available_bus.empty? ? nil : @available_bus[:bus ].id %>");
$("#people-group").css("display", "block");
// disable make booking button
$("#make-booking input").prop('disabled', false);


